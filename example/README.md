# Example Workspaces

This directory contains disposable example workspaces for validating Ship features end to end.

Current workspace:

- `projects-e2e/`: project workflow validation (CLI + MCP + UI) with local `.ship/` state ignored by git.
