# Example Projects

Project fixtures used by the `examples/e2e` suite and available for manual
compiler/export experiments.

- `rust-cli/` - Rust CLI fixture.
- `webapp-nextjs/` - Next.js web app fixture.

These are intentionally small and deterministic so test assertions stay stable.
