# Model Context Protocol (MCP) Configuration
# - Define servers, tools, and resources for your agents.

[mcp]
# Ship runtime MCP server is enabled by default.
# This gives agents workspace-aware tools that map to Ship workflow primitives.
[mcp.servers.ship]
name = "Ship Runtime"
command = "ship"
args = ["mcp", "serve"]
