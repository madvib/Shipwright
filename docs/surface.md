# Ship Surface

Generated by `ship surface`. Do not edit manually.

## CLI Commands

### `ship init`

Scaffold .ship/ in the current project, or configure ~/.ship/ globally

- `--from <url>` — Fetch a JSON config bundle and scaffold from it
- `--global` — Configure ~/.ship/ instead of current project
- `--provider <id>` — Default provider (claude, gemini, codex, cursor)
- `--force` — Overwrite existing .ship/

### `ship login`

Authenticate with getship.dev

### `ship logout`

Sign out

### `ship whoami`

Show current identity

### `ship use <agent-id>`

Activate an agent (compiles immediately)

- `--path <dir>` — Bind to this path instead of cwd

### `ship status`

Show active agent and compilation status

- `--path <dir>` — Target directory

### `ship compile`

Compile the active agent to provider-native config files

- `--provider <id>` — Compile for a specific provider only
- `--dry-run` — Preview without writing files
- `--path <dir>` — Project root

### `ship agents`

Manage agents

- `list [--local] [--project]` — List available agents
- `create <name> [--global]` — Create a new agent
- `edit <name>` — Open agent in $EDITOR
- `delete <name>` — Delete an agent
- `clone <source> <target>` — Clone an agent under a new ID

### `ship skills`

Manage agent skills

- `add <source> [--skill <id>] [--global]` — Install a skill
- `list` — List installed skills
- `remove <id> [--global]` — Remove a skill
- `create <id>` — Scaffold a new skill

### `ship mcp`

Manage MCP servers

- `serve [--http] [--port <n>]` — Run the Ship MCP server
- `add <id> --url <url>` — Register an HTTP/SSE server
- `add-stdio <id> <command> [args...]` — Register a stdio server
- `list` — List configured servers
- `remove <id>` — Remove a server

### `ship install`

Install all dependencies in .ship/ship.toml

- `--frozen` — Fail if lockfile would change

### `ship add <package>`

Add a dependency to .ship/ship.toml and install it

### `ship convert <source>`

Convert provider config files (CLAUDE.md, .cursor/) into .ship/ format

### `ship validate`

Validate .ship/ config before compile

- `--agent <id>` — Validate a single agent
- `--json` — Emit errors as JSON
- `--path <dir>` — Project root

### `ship events list`

Query the project event log

- `--since <time>` — Since timestamp or relative (e.g. 1h, 24h)
- `--actor <name>` — Filter by actor
- `--entity <type>` — Filter by entity type
- `--action <name>` — Filter by action
- `--limit <n>` — Max events (default 50)
- `--json` — Output as JSON

### `ship docs [topic]`

Show detailed help for a topic (run `ship docs topics` to list)

### `ship view`

Browse and manage project state in the terminal UI

## MCP Core Tools

Tools always available regardless of active agent:

- `activate_workspace`
- `append_job_log`
- `claim_file`
- `complete_workspace`
- `create_adr`
- `create_job`
- `create_note`
- `create_workspace`
- `end_session`
- `get_file_owner`
- `list_events`
- `list_jobs`
- `list_skills`
- `list_stale_worktrees`
- `list_workspaces`
- `log_progress`
- `open_project`
- `set_agent`
- `start_session`
- `update_job`
- `update_note`
