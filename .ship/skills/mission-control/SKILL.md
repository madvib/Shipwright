---
name: mission-control
stable-id: mission-control
description: "[DEPRECATED] Replaced by cmdr + mesh-based agent networks. File-based coordination is no longer needed."
deprecated: true
---

# Deprecated

Mission control was file-based multi-agent coordination for environments without MCP. Now that mesh-based agent networks handle communication, this skill is redundant.

Use the `cmdr` skill with `dispatch` and `gate` instead.
