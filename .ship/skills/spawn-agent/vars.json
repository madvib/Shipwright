{
  "$schema": "https://agentskills.io/schemas/vars/v1.json",
  "terminal": {
    "type": "enum",
    "default": "auto",
    "storage-hint": "user",
    "values": ["auto", "wt", "iterm", "tmux", "gnome", "vscode", "manual"],
    "label": "Terminal",
    "description": "Terminal to open when dispatching agents. auto = detect from environment."
  },
  "worktree_dir": {
    "type": "string",
    "default": "~/dev/ship-worktrees",
    "storage-hint": "user",
    "label": "Worktree base directory",
    "description": "Base directory where git worktrees are created."
  },
  "confirm_on_dispatch": {
    "type": "bool",
    "default": false,
    "storage-hint": "user",
    "label": "Confirm before dispatch",
    "description": "Show job spec summary and prompt y/n before launching the agent."
  }
}
