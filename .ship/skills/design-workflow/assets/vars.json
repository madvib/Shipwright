{
  "$schema": "https://getship.dev/schemas/vars.schema.json",
  "server_port": {
    "type": "string",
    "default": "8765",
    "storage-hint": "global",
    "label": "Preview server port",
    "description": "Port for the local HTTP preview server. Must match visual-brainstorm server_port if used together."
  }
}
