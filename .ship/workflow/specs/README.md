# workflow/specs/

Specs are managed in Ship state (SQLite), not as hand-authored markdown files in git.

Use CLI/MCP to manage specs:

- `ship spec create "<title>" -c "<content>"`
- `ship spec list`
- `ship spec get <file_name>`

This directory is kept only for workflow documentation.
