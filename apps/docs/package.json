{
  "name": "@ship/docs",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "collect": "npx tsx scripts/collect-skill-docs.ts",
    "predev": "npm run collect",
    "dev": "astro dev",
    "prebuild": "npm run collect",
    "build": "astro build",
    "preview": "astro preview",
    "build:npm": "npm run collect && npx astro build"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.38.2",
    "astro": "^6.1.1",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0"
  }
}
