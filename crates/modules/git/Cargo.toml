[package]
name = "ship-module-git"
version = "0.1.0"
edition = "2024"

[dependencies]
runtime = { path = "../../core/runtime" }
ship-module-project = { path = "../project" }
anyhow = "1.0"
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.3"
